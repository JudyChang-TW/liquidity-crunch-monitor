# Environment Variables for Liquidity-Crunch-Monitor
#
# SECURITY NOTICE:
# - NEVER commit .env or .env.local files to Git
# - These files are already in .gitignore
# - Production secrets should be stored in your deployment platform's Secrets Manager
#   (AWS Secrets Manager, GitHub Secrets, HashiCorp Vault, etc.)

# ============================================================================
# LOCAL DEVELOPMENT SETUP
# ============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your local development credentials
# 3. Source it before running: source .env.local  OR  export $(cat .env.local | xargs)

# ============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================================
# PostgreSQL database password
#
# Local Development:
#   Use any password you want for your local PostgreSQL instance
#   Example: DB_PASSWORD="dev_password_123"
#
# Production:
#   MUST be a strong, randomly generated password (30+ characters)
#   Store in your deployment platform's Secrets Manager
#   Example: DB_PASSWORD="x8#kL9v$m2Pq@zR5nT3wY6uE9rP2sD5f"
DB_PASSWORD=

# ============================================================================
# BINANCE API CREDENTIALS (OPTIONAL - for authenticated endpoints)
# ============================================================================
# Most features work without API credentials (public orderbook data)
# Only needed if you want to:
# - Access private account data
# - Increase rate limits
# - Use authenticated endpoints

# BINANCE_API_KEY=
# BINANCE_API_SECRET=

# ============================================================================
# EXAMPLE SETUP FOR LOCAL DEVELOPMENT
# ============================================================================
# For local testing with Docker Compose:
#
# 1. Start PostgreSQL:
#    docker run --name postgres-dev -e POSTGRES_PASSWORD=dev123 -p 5432:5432 -d postgres:15-alpine
#
# 2. Set environment variable:
#    export DB_PASSWORD="dev123"
#
# 3. Run the application:
#    python main.py

# ============================================================================
# PRODUCTION DEPLOYMENT EXAMPLES
# ============================================================================
#
# Docker:
#   docker run -e DB_PASSWORD="$DB_PASSWORD" liquidity-monitor
#
# GitHub Actions:
#   - name: Run tests
#     env:
#       DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
#
# AWS ECS:
#   Use AWS Secrets Manager and reference in task definition
#
# Kubernetes:
#   Use Kubernetes Secrets and mount as environment variables
